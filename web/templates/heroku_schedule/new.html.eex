<h2>New Heroku schedule</h2>

<%= render "form.html", changeset: @changeset,
                        action: heroku_schedule_path(@conn, :create) %>

<%= link "Heroku Schedules", to: heroku_schedule_path(@conn, :index) %>
